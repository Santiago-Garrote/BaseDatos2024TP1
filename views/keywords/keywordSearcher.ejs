<!DOCTYPE html>
<html>
<head>
    <title>MovieWeb-Keyword</title>
    <style>
        body {
            font-family: 'Arial', sans-serif;
            background-color: #f5f5f5;
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
            height: 100vh;
            margin: 0;
        }

        h1 {
            font-size: 36px;
            color: #333333;
            margin-bottom: 20px; /* Agregamos margen inferior para separar del input */
        }

        form {
            text-align: center;
        }

        input[type="text"] {
            padding: 10px;
            width: 300px;
            border: 1px solid #ccc;
            border-radius: 4px;
            font-size: 18px;
            margin-right: 10px;
        }

        button[type="submit"], button#MovieButton {
            background-color: #0078d4;
            color: white;
            border: none;
            border-radius: 4px;
            padding: 10px 20px;
            font-size: 18px;
            cursor: pointer;
            margin-top: 10px; /* Añado margen superior para separar los botones */
        }

        button[type="submit"]:hover, button#MovieButton:hover {
            background-color: #0056b3;
        }
    </style>
</head>
<body>
<h1>MovieWeb-Keyword</h1>
<% if (userLoggedIn) { %>
    <div id="user"><a href="/userProfile">Mi perfil</a></div>
<% } else { %>
    <div id="user"><a href="/login">Login</a></div>
<% } %>
<br><br>
<form action="/buscar-keyword" method="GET">
    <input type="text" name="q" placeholder="Buscar por keywords..." />
    <button type="submit">Buscar</button><br>
    <!-- Botón que redirige a la página de búsqueda por película, con estilo similar al de Buscar -->
    <button type="button" id="MovieButton" onclick="window.location.href='http://localhost:3000/index'">Buscar por Película</button>
</form>
</body>
</html>
